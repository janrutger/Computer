# Optimization Candidate: FP.from_int

## Current Implementation (Function Call)
    call @FP.from_int
    ustack A $DATASTACK_PTR

## Target Implementation (Inline Macro)
    ldm A $SCALE_FACTOR
    ustack B $DATASTACK_PTR
    mul A B

## Special Case (Full Optimization)
    # When input is in A and result returns to A
    # Old:
    stack A $DATASTACK_PTR
    call @FP.from_int
    ustack A $DATASTACK_PTR

    # New:
    ld B A
    ldm A $SCALE_FACTOR
    mul A B


    