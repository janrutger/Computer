# .FUNCTIONS
@gcd
:gcd_while_start_0
    call @rt_dup
    ldi A 0
    call @push_A
    call @rt_neq
    call @pop_A
    tst A 0
    jmpt :gcd_while_end_0
    call @rt_swap
    call @rt_over
    call @rt_mod
    jmp :gcd_while_start_0
:gcd_while_end_0
    call @rt_drop
    ret

# .DATA
% $_start_memory_ 0
